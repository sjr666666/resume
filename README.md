# 石敬荣的在线简历

这是一个可直接部署到 GitHub Pages 的静态简历页面。

## 发布到 GitHub Pages

1. 在 GitHub 新建一个仓库，例如 `resume`。
2. 上传 `index.html` 和本 README。
3. 进入仓库的 `Settings` -> `Pages`。
4. 在 `Build and deployment` 中选择 `Deploy from a branch`，分支选择 `main` / 根目录。
5. 保存后等待几分钟，GitHub 会生成访问地址。

页面已经隐藏手机号，仅保留邮箱和 GitHub 项目链接。
